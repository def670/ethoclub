/**
 * Meant to either localize a number with toLocaleString or return an Integer
 * localization accepts 3 arguments
 *  - significantDigits
 *  - minimumFractionDigits
 *  - maximumFractionDigits
 */
export declare function normalizeNumber(decimal: number, arbitraryPrecision: number, sign: number, locale: string, { significantDigits, minimumFractionDigits, maximumFractionDigits }: {
    significantDigits?: number | undefined;
    minimumFractionDigits?: number | undefined;
    maximumFractionDigits?: number | undefined;
}): string | number | undefined;
export declare function extractIntPart(decimal: number, range: number, numberOfDigits: number): number;
